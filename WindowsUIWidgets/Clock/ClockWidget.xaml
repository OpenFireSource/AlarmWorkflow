<UserControl x:Class="AlarmWorkflow.Windows.UIWidgets.Clock.ClockWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="370">

    <StackPanel HorizontalAlignment="Center" Name="stackPanel1" VerticalAlignment="Center" Orientation="Horizontal">
        <TextBlock HorizontalAlignment="Stretch" Margin="5,0,0,0" Foreground="{Binding ForeColor}" TextAlignment="Center" Text="{Binding Time ,StringFormat={}{0:HH\\:mm\\:ss}}" VerticalAlignment="Center" FontSize="40" FontWeight="Bold" />
        <Separator Height="2" Margin="5,0,5,0" HorizontalAlignment="Center">
            <Separator.LayoutTransform>
                <RotateTransform Angle="90" />
            </Separator.LayoutTransform>
        </Separator>
        <TextBlock FontSize="40" FontWeight="Bold" Text="{Binding AlarmTime, StringFormat={}{0:hh\\:mm\\:ss}}" Foreground="{Binding ForeColor}" TextAlignment="Center" />
    </StackPanel>
</UserControl>