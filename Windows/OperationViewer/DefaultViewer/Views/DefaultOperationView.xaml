<UserControl x:Class="AlarmWorkflow.Windows.DefaultViewer.Views.DefaultOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AlarmWorkflow.Windows.DefaultViewer.Views">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- General inforamtion -->
        <views:GeneralInformationControl Grid.Column="0" Margin="2" />

        <!-- Route Control -->
        <views:RouteControl Operation="{Binding Operation}" Grid.Column="1" Margin="2" />
    </Grid>

</UserControl>
